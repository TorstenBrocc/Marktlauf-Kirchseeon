<?php
/**
 * CI & Design — abgelöst durch den Design-System-Browser (orga/design_system.php).
 *
 * Diese Seite ist stillgelegt und leitet dauerhaft dorthin um; die URL bleibt für alte
 * Bookmarks/Links gültig (kein 404). Der frühere Token-Referenz-Inhalt (Farb-Tokens aus
 * css/base.css) lebt jetzt fachlich gruppiert im DS-Browser (Sektion „Farben").
 */

declare(strict_types=1);

header('Location: design_system.php', true, 301);
exit;
